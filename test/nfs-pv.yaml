apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-test
  # namespace: nfs
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  nfs:
    server: nfs-server.default.svc.cluster.local
    path: "/test"
  mountOptions:
    - nfsvers=4.2
