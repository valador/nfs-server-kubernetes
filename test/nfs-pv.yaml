apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-test
  # namespace: nfs
spec:
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  nfs:
    server: dev-srv.home.lan
    path: "/"
  # mountOptions:
  #   - nfsvers=4.2
